// Tuner page for the Serenity Strings website.

import React from 'react'

import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Tuner',
};

const TunerPage = () => {
  return (
    <div>Tuner Page</div>
  )
}

export default TunerPage
// Metronome page for the Serenity Strings website.

import React from 'react'

import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Metronome',
};

const MetronomePage = () => {
  return (
    <main className = "">
      Metronome Page
    </main>
  )
}

export default MetronomePage